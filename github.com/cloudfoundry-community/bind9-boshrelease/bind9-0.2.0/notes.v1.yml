{
  "Content": "The `ttar` utility is now embedded in the master job, instead of\nas a separate package, to allow other releases to use ttar without\nconflicting on the BOSH global namespace for packages.\n"
}