{
  "Content": "The `sslproxy.blacklisted_domains` property was added to allow users to list domain suffixes that they wish to blacklist from being passed through the reverse proxy. This is mostly useful for hiding internal domains that co-exist with public domains on the target of the reverse proxy.\n"
}