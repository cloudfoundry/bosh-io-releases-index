{
  "Content": "This patch release makes some minor changes to the [`if_it_looks_like_json`](https://github.com/logsearch/logsearch-boshrelease/blob/f2863ff4b602103118f3f12f424ed6d42d9cd43e/src/logsearch-config/src/logstash-filters/if_it_looks_like_json.conf) parsing rules to:\n- Add `@source.program` to the list of fields to extract a JSON key name from\n- When JSON contains UNIX timestamps with nanosecond precision; extract to `@timestamp` with millisecond precision and store the original ns value `@timestamp_ns` \n"
}