{
  "Content": "* winc-network will configure container network adapter with same MTU as active host network adapter if not configured via bosh property (or set to \"0\").\r\n* Fixed a bug where processes within containers were able to communicate with other containers in the cell."
}