{
  "Content": "**Changes**:\n\nNo functional changes.  This release primarily just removes \"PID guard\" logic from bash as it was being duplicated in `confab` anyways, and removes Ruby logic from the job templates to determine the instance's IP, and uses BOSH's `spec.address` instead.\n"
}